{
  "name": "rough-time",
  "version": "0.0.1",
  "description": "Gives you a rough idea of the time",
  "main": "dist/route-time.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --compilers js:babel-core/register test/**/*.spec.js",
    "coverage": "babel-node node_modules/.bin/babel-istanbul cover ./node_modules/mocha/bin/_mocha -- --timeout 30000 test/*.spec.js",
    "build": "node_modules/.bin/babel src --out-dir dist",
    "prepublish": "npm run build"
  },
  "author": "Ben Thomas (https://benhjt.github.io)",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.6.4",
    "babel-istanbul": "^0.6.1",
    "babel-preset-es2015": "6.6.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  }
}
